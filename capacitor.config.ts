import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prjCigarros2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
